
Physics calculations with newtypes wrapping Doubles that have Meters, Seconds. 
Or better as types (aliases)?

Recursion: get the integer powers of a number (done). Do it again in tail-recursive form.

Recursion: find if all the elements of a list are even.

Recursion: find the maximum value in a list & find the minumum.

Recursion + H.O.F. find the maximum or minimum in a list, pass in the comparison fn.

Recursion plus IO: Tower of Hanoi from JS Good Parts

Recursion plus IO: Coin Problem from SICP

Data Types: Coins

IO: write a ``guess my number game'' that tracks the number of guesses made.
